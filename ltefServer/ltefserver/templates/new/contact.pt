
<div metal:use-macro="layout">
    <div metal:fill-slot="content">

    <div class="container">

      <h1>Contact Us</h1>

      <form method="POST" action="${request.current_route_url()}" tal:condition="state != 'sent'">
        <p>You can use this form to send us a message.</p>
        <div>
          <textarea name="txtComment" rows="7" cols="80" class="span10"></textarea>
        </div>
        <button id="btnSubmit" type="submit" class="btn btn-primary btn-outline btn-m">Send</button>
      </form>
      <div tal:condition="state == 'sent'" class="alert alert-info">Your message has been sent.</div>
    </div> <!-- /container -->
  </div>
</div>
